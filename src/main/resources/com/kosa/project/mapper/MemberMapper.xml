<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kosa.project.mapper.MemberMapper">

	<select id="find" resultType="com.kosa.project.domain.MemberVO">
		SELECT * FROM MEMBER WHERE IDX= #{idx}
	</select>

	<insert id ="insert">
		<selectKey resultType = "int" order = "BEFORE" keyProperty = "idx">
			SELECT member_seq.nextval FROM DUAL		
		</selectKey>
		insert into member (idx, login_id, password, name, email, phone, birth)
		values (#{idx},#{loginId},#{password},#{name},#{email},#{phone}, #{birth})		
	</insert>
	
	<delete id="delete">
		DELETE FROM MEMBER WHERE idx = #{idx} 
	</delete>
	
	<update id="update">
		UPDATE MEMBER SET
		login_id = #{loginID},
		password = #{password},
		name= #{name},
		email = #{email},
		phone = #{phone},
		birth = sysdate
		WHERE idx = #{idx};
		
	</update>



	
</mapper>